<Window x:Class="C_DANE_reports.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C_DANE_reports"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
		<WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
			<Button Name="btnOpenFile" Click="btnOpenFile_Click">Open file</Button>
			<ListView x:Name="ListViewPeople">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="REGION" Width="100" DisplayMemberBinding="{Binding Path=Region}"/>
						<GridViewColumn Header="CODIGO DANE" Width="120" DisplayMemberBinding="{Binding Path=CodigoDane}"/>
						<GridViewColumn Header="DEPARTAMENTO" Width="120" DisplayMemberBinding="{Binding Path=Departamento}"/>
						<GridViewColumn Header="CODIGO DANE" Width="120" DisplayMemberBinding="{Binding Path=CodigoDane2}"/>
						<GridViewColumn Header="MUNICIPIO" Width="100" DisplayMemberBinding="{Binding Path=Municipio}"/>
					</GridView>
				</ListView.View>
			</ListView>
		</WrapPanel>
    </Grid>
</Window>
